/**
 * Demo 03 — Form Labels
 * No interactive JS needed — the demo is purely HTML/CSS.
 * The broken panel's placeholder-disappearing behavior is native browser behavior.
 */

export function init(_sectionEl) {
  // Static demo — no JS initialization required
}
