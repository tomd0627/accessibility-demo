/**
 * Demo 09 — Skip Link
 * Static demo — the page's own skip link IS the demonstration.
 * No additional JS needed.
 */

export function init(_sectionEl) {
  // The page-level skip link at the top of <body> is the live demo.
  // Press Tab as the first action on the page to see it appear.
}
