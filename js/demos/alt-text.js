/**
 * Demo 02 — Alt Text
 * No interactive JS needed for this demo — it's purely HTML/CSS.
 * This module exists to satisfy the IntersectionObserver loader pattern
 * and could be used to add future interactivity.
 */

export function init(_sectionEl) {
  // Static demo — no JS initialization required
}
